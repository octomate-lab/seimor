# Directivas de contexto http {} — el include global de conf.d/*.conf las carga automáticamente
limit_req_zone $binary_remote_addr zone=seimor_general:10m rate=20r/s;
limit_req_zone $binary_remote_addr zone=seimor_wp_login:10m rate=5r/m;
limit_conn_zone $binary_remote_addr zone=seimor_conn:10m;
